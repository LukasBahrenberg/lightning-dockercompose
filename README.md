# lightning-dockercompose

This repository is a simple lightning node based on docker-compose. I tested it on an Ubuntu 22.04 VPS with a static IP. The setup is based on a number of Python scripts that run shell commands via subprocess and do some other things.
